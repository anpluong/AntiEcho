const Articles = require('./articleModel.js');
const Queries = require('./user.js');


userController.addToUser = (req, res, next) => { // needs to be fixed
    
    
    next();
};

module.exports = userController;